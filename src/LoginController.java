public class LoginController {
    User user;

    public Boolean login(String password) {
        // check txt for password and return result
    }

    public void setPassword(String password) {
        // replace password in txt
        user.password = password;
    }
}
